 #include<stdio.h>
 #include<math.h>
  int main()
{
  int a,b,c;
  float r1,r2,t;
  printf("enter the three numbers:");
  scanf("%d%d%d",&a,&b,&c);
  t=(pow(b,2))-(4*a*c);
  r1=((-b)+sqrt(t))/(2*a);
  r2=((-b)-sqrt(t))/(2*a);
  printf("the roots are %f,%f",r1,r2,t);
if(t>0)
  printf("the roots are real and unequal:");
else if(t=0)
   printf("the roots are real and equal:");
 else if(t<0)
  printf("the roots are imaginary:");
  }
